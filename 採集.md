## 【資源點生成與刷新規則】
- **地城重新整理（Reset On Entry）**：與一次性寶箱不同，當玩家離開並**重新進入地下城時，採集點（如：水晶礦）會全面刷新（Respawn）**。
- **隨機位置（Dynamic Location）**：採集點在該層地圖中的出現位置**不固定**。系統會於每層預設的多個「可能衍生點」中隨機挑選位置生成，增加探索的動態感。
- **總量控制（Volume Balance）**：雖然位置隨機，但**每一層出現的採集點總數量會嚴格控制在企劃設定的區間內**。
    - _例如：第 1 層固定隨機生成 2~3 個水晶點；第 2 層固定隨機生成 3~5 個水晶點。_

## 【採集操作與工具判定】
- **工具限制（Tool-Gate）**：採集點無法透過普通武器揮擊或空手按按鍵獲得。玩家**必須裝備特定的採集工具**（如：礦鎬，請參考[[玩家角色]]規格）才能進行採集。
- **操作行為**：當玩家手持正確工具接近採集點時，面向採集點按下攻擊/使用鍵，角色將播放「挖掘/採集」的動作動畫。

## 【產出物與數量決定機制】
- **種類固定（Fixed Drop Type）**：特定採集點對應產出的素材種類是 100% 固定的（例如：紫色水晶點只會產出「紫水晶結晶」，不會隨機噴出鐵礦或草藥）。
- **數量浮動與工具加成（Dynamic Yield）**：每次採集獲得的素材數量在一個範圍內浮動，且**產出上限與下限完全取決於玩家當前裝備的效果**：
    - _初階工具（如：劣質礦鎬）_：單次採集產出數量較少（例如：3 ~ 5 個）。
    - _高階工具（如：精鋼礦鎬）_：單次採集產出數量顯著提升（例如：5 ~ 10 個）。
- **結算流程**：採集完成後，素材將如同寶箱規則，**直接自動加進玩家背包**，並即時累加顯示於底部的戰利品 UI（請參考[[戰利品]]規格）中。