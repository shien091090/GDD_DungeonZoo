![[zoo_enclosure_management_ui.png]]
## 【介面定位與顯示架構】
展示區管理頁面（可由主介面「功能按鈕區塊」之【展區管理】按鈕開啟）為一個全螢幕或中央彈出式之大型管理介面。頁面核心以 **「數據表格（Table）」** 呈現全園區所有已建造展區的即時營運與維護數據，提供玩家宏觀監控與迅速操作的管道。

## 【數據表格欄位規格】
表格由左至右包含以下六大欄位：

| **欄位名稱**   | **支援排序** | **顯示格式與詳細規格**                                                                                                                                                                                                                      |
| ---------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **編號**     | **是**    | **流水號格式**：固定從 `001` 開始編號（如：`001`、`002`、`003`...），依據展區建造順序預設生成。                                                                                                                                                                     |
| **魔物組成**   | **是**    | **圖示與數量組合**：顯示該展區收容之魔物種類與數量（不顯示魔物個人名稱）。<br>• **單一種族**：`【魔物頭像Icon】× {數量}`（例如：`[史萊姆Icon] × 3`）。<br>• **多種族混養**：採**跳行（Multi-line）** 排版呈現：<br>`【魔物頭像Icon A】× {數量}`<br>`【魔物頭像Icon B】× {數量}`                                             |
| **堅固度**    | **是**    | **Slider 進度條 + 百分比**：<br>• 左側為 Slider 進度條（與[[動物園展區]]堅固度條顏色與視覺一致）。<br>• 右側顯示百分比文字（格式：`{數字}%`，強制僅顯示至整數位）。<br>• 當堅固度降至 0% 時（破籠狀態），進度條歸零，右側顯示 `0%`。                                                                                    |
| **環境**     | 否        | **圖示 + 文字**：格式為 **`【環境Icon】 {環境名稱}`**（例如：`[雪地Icon] 雪地`、`[森林Icon] 森林`）。                                                                                                                                                             |
| **受歡迎程度**  | **是**    | **數值 + 評語**：顯示格式為 **`{數值}/100 {評語}`**。<br>• **數值計算**：綜合當天營業期間遊客在該展區旁的駐足時間與停留人數即時計算（範圍：`0 ~ 100`）。<br>• **數字區間評語對照表**：<br>- `0 ~ 20`：`冷清`<br>- `21 ~ 40`：`有點無聊`<br>- `41 ~ 60`：`有點人氣`<br>- `61 ~ 80`：`頗受歡迎`<br>- `81 ~ 100`：`非常受歡迎` |
| **操作按鈕區塊** | 否        | **配置雙按鈕**：每個展區橫列最右側配置兩顆實體操作按鈕：<br>1. **【詳細】按鈕**<br>2. **【定位】按鈕**                                                                                                                                                                   |

## 【按鈕互動與 UI 層級邏輯】
在任何一列展區資料中，玩家點擊「操作按鈕區塊」時之行為如下：
### 1. 【詳細】按鈕
- **點擊行為**：開啟該展區獨立的 **「展區屬性面板（展區管理介面 UI）」**。
- **鏡頭行為**：**鏡頭保持當前位置完全不動**。
- **UI 層級（Depth/Z-Order）**：獨立的「展區屬性面板」會以更高的 UI 層級**直接覆蓋在「展示區管理頁面」之上**。當玩家關閉「展區屬性面板」後，會順暢回到「展示區管理頁面」。
### 2. 【定位】按鈕
- **點擊行為**：**立即關閉**「展示區管理頁面」。
- **鏡頭行為**：將遊戲攝影機（Camera）的畫面中心點**滑順移動並聚焦對齊（Focus）至該目標展區上**，方便玩家直接在主場景中進行觀察或建設。

## 【欄位排序機制（Sorting System）】
### 1. 支援排序之欄位
僅限 **【編號】**、**【魔物組成】**、**【堅固度】**、**【受歡迎程度】** 四個欄位標題旁設有排序箭頭按鈕。
### 2. 視覺狀態（箭頭 Icon 狀態）
- **高亮亮起（Active State）**：當前正作為排序依據的欄位，其按鈕呈現高亮色彩（如金黃色/白亮色）。
    - `▲`（正三角形）：代表由低到高（升冪排序 Ascending）。
    - `▼`（倒三角形）：代表由高到低（降冪排序 Descending）。
- **壓灰停用（Inactive State）**：未被選中的其餘可排序欄位，其箭頭按鈕呈現**壓灰（Grayed out）** 的預設升冪 `▲` 狀態。
### 3. 預設與切換邏輯
- **初次開啟預設**：預設以 **【編號】** 欄位進行排序，箭頭顯示為高亮正三角形 `▲`（由編號 `001` 由低至高排序），其餘欄位箭頭皆為壓灰 `▲`。
- **點擊當前高亮欄位**：若玩家再次點擊「當前高亮」的欄位箭頭，該箭頭會在 **`▲`（由低到高）與 `▼`（由高到低）之間來回切換**。
- **點擊其他壓灰欄位**：若玩家點擊「其他壓灰」的欄位箭頭：
    1. 原高亮欄位按鈕轉為壓灰 `▲`。
    2. 新點擊的欄位按鈕解除壓灰狀態，高亮顯示為正三角形 `▲`，並立即將表格切換為該欄位之「由低到高」排序。

## 【跨階段權限與運作規則】
- **全階段功能一致**：**準備階段**與**營業階段**下，展示區管理頁面之**所有功能、按鈕與數據顯示完全無區別**。
- **即時數據更新**：在營業階段開啟此頁面時，【堅固度】、【魔物組成】 與 【受歡迎程度】 欄位之數據皆會隨場景真實狀態進行**即時（Real-time）滾動刷新**。