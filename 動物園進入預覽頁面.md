![[zoo_zone_enter_preview_panel.png]]
## ① 動物園圖示 ----------------------------------------
顯示該園區當前外觀的預覽視窗。視覺表現僅與「園區等級」掛鉤，並具備狀態覆蓋機制以即時反應園區的營業狀態。
### 視覺外觀與等級連動
- **靜態圖示切換：** 預覽圖示為純靜態圖片，不讀取、不顯示玩家在園區內的實際建築或魔物配置。
- **等級外觀包（Skin）：** 圖片內容隨「園區等級（Lv）」改變。當園區升級時，圖示自動切換為對應等級的豪華外觀插圖（如：Lv.1 為木造破舊帳篷，高等級則變為華麗魔幻風格）。
### 狀態覆蓋機制（維修中）
- **觸發條件：** 當園區因魔物失控導致展示區損壞、無法營業時觸發。
- **視覺表現：** * 底層的動物園預覽圖示會**壓暗（亮度降低/套用灰階濾鏡）**。圖示正中央會**覆蓋（Overlay）一張「維修中」的 UI 標籤貼紙**（圖示/文字標記）。

## ② 動物園名稱 ------------------------------------------
顯示並允許玩家自訂該園區的名稱，並同時顯示當前園區等級。此區塊具備多語系防破圖機制與狀態顏色連動。
### UI 結構與排列
- **獨立組件設計：** 「園區名稱」與「等級標籤 `(LvX)`」為兩個獨立的 UI 文字組件。
- **字數縮放（Best Fit）：** 園區名稱設有字數限制。若玩家輸入的字串過長，文字組件將啟動自動縮放功能（Best Fit），使字體自動縮小以適應固定的文字框寬度。
### 自訂名稱功能（鉛筆按鈕）
- **觸發更改：** 名稱右側配置一個「鉛筆圖示」按鈕，玩家點擊後將彈出輸入框（Input Field）小視窗以供修改。
- **空白防呆機制：** 每個園區皆有系統內建的預設名稱（如：海角園區）。若玩家將文字完全刪除、或僅輸入純空白字元並按下確認，系統將自動將名稱還原為預設名稱。
- **彈窗關閉邏輯：** 當自訂名稱的輸入框小彈窗開啟時：
    - 點擊彈窗內的「確認」鈕：儲存變更並關閉彈窗。
    - **點擊彈窗外部的空白區域** 或 **按下鍵盤 ESC 鍵**：視為「取消變更」，直接關閉彈窗，名稱保持原樣。
### 營業狀態連動
- **正常狀態：** 文字顯示為預設標準色（如圖片中的黑/深褐色）。
- **維修狀態：** 當園區因魔物失控導致損壞、無法營業時，名稱文字顏色將**切換為紅色**。

## ③ 動物園資訊 ----------------------------------------
即時讀取並顯示該園區的核心經營數據。此區塊作為數據的高級摘要，數值在維修狀態下仍保持可讀性。
### 數據定義與讀取機制
- **單次讀取更新：** 由於遊戲機制需進入園區並點擊「開始營業」才會推動經營模擬，在大地圖期間園區皆處於休息狀態（詳細請參考[[基本設計(動物園經營)]]規格）。因此，此面板數據**僅在開啟預覽頁面的瞬間讀取存檔並更新一次**，停留在此畫面時數值不會即時跳動。
- **指標定義：**
    - **規模等級：** 顯示格式為 `[當前等級]/[最大等級]`（如：1/5）。升級機制由園區內消耗金幣觸發（詳細請參考[[基本設計(動物園經營)]]規格）。
    - **名聲：** 顯示當前動態浮動的名聲值。此數值會隨魔物表演提升，或因魔物失控事件而下降。
    - **累積來客數：** 純紀錄與成就感展示用的累計數值。
    - **員工數：** 顯示當前於園區內雇用的員工總數，數值無上限。
### UI 顯示與格式化
- **千分位支援：** 為了提升後期大數值的閱讀性，除「規模等級」外，所有純數字欄位（名聲、累積來客數、員工數）只要達千位數以上，皆須強制格式化並顯示千分位撇號。
- **營業狀態連動：** 當園區因損壞進入「無法營業（維修中）」狀態時，此區塊的所有數據與文字**依然保持原樣正常顯示**（不壓暗、不變紅），以利玩家在進入前評估損失。

## ④ 展示魔物列表預覽 -------------------------------------
以種類合併與動態排序的形式，預覽當前園區內配置的魔物。此區塊具備動態排版、點擊跳轉圖鑑功能，並隨資料狀態自動調整顯示。
### 資料聚合與動態排序邏輯
- **種類合併堆疊：** 相同種類的魔物不論在園區內哪個展示位，一律合併為同一個圖示，並於右上角累加總數量（如：`x2`）。
- **最新置頂排序（Recency Priority）：** 格子排序採「越晚放入園區的魔物排在越前面（左上角第一格）」。
    - **增量變更案例：** 若玩家放入一隻園區內已存在的魔物種類，該魔物的數量標籤增加，且**該魔物圖示格子會動態位移至排列的最前方**（第一格）。
- **自動排版：** 採用網格佈局（Grid Layout），格子從左到右、從上到下自動生成。
### 交互反饋與導連機制
- **滑鼠懸停（Hover）：** 滑鼠游標移至魔物格子上方時，格子外框亮起以提示玩家可進行點擊。
- **點擊行為（Click）：** 點擊後，會在當前大地圖畫面上**直接彈出一個「魔物資訊頁面」彈窗**。
    - 此彈窗會蓋在預覽頁面之上，**不會**將玩家傳送進園區內部，底層的預覽頁面亦不會關閉。
    - 打開時，資訊頁面會自動定位並切換至該點擊魔物的專屬分頁。
### 空狀態與全域捲動連動
- **隱藏機制：** 若該園區內目前「沒有配置任何魔物」，整個魔物列表區域將完全留白，且**上方的「園區魔物」中央分隔線與文字標籤會同步隱藏**，使下方區塊（販賣素材）自動往上遞補。
- **捲動架構（與編號⑦連動）：** 魔物列表本身不具備獨立捲動功能。此區塊與整體面板共用同一個全域滾動視窗（Global Scroll View）。當魔物與素材總高度超出外框時，透過拉動 ⑦ 卷軸，**整張皮紙內容會一起往上捲動**。